# Dependencies — never commit this (100k+ files, 200MB+)
node_modules/

# Build output — generated by "npm run build", not source code
dist/

# Local environment variables — may contain secrets
.env
.env.local

# Editor clutter
.DS_Store
.vscode/